# 脚本目录说明

本目录包含 PrecomControl 项目的所有脚本文件，按功能分类组织。

## 📁 目录结构

```
scripts/
├── core/          # 核心脚本（启动、部署、证书生成）
├── tools/         # 工具脚本（诊断、检查）
└── i18n/          # 国际化脚本（翻译管理）
```

## 🚀 核心脚本 (core/)

### `start.ps1` - 统一启动脚本
启动 PrecomControl 应用，支持多种模式。

**使用方式**：
```powershell
# 生产模式（默认）
.\scripts\core\start.ps1

# 开发模式
.\scripts\core\start.ps1 -Mode dev

# 服务模式（用于 NSSM）
.\scripts\core\start.ps1 -Mode service

# 带参数
.\scripts\core\start.ps1 -Mode production -Threads 16 -HTTPS
```

### `deploy.ps1` - 统一部署脚本
自动化部署流程，包括 Nginx 配置、证书生成、防火墙设置等。

**使用方式**：
```powershell
# 标准部署（内网IP）
.\scripts\core\deploy.ps1 -Mode standard -IP 10.78.44.3

# 标准部署（域名）
.\scripts\core\deploy.ps1 -Mode standard -Domain cc7projectcontrols.com

# 快速部署（无需Nginx）
.\scripts\core\deploy.ps1 -Mode quick

# 完整部署
.\scripts\core\deploy.ps1 -Mode full -Domain cc7projectcontrols.com
```

### `generate_cert.ps1` - 证书生成脚本
生成 SSL 证书（自签名或 Let's Encrypt）。

**使用方式**：
```powershell
# 生成IP证书（内网）
.\scripts\core\generate_cert.ps1 -IP 10.78.44.3

# 生成域名证书（公网）
.\scripts\core\generate_cert.ps1 -Domain cc7projectcontrols.com
```

## 🛠️ 工具脚本 (tools/)

### `check_deployment.ps1` - 部署状态检查
检查系统各组件运行状态（Python、依赖、数据库、Flask、Nginx等）。

**使用方式**：
```powershell
.\scripts\tools\check_deployment.ps1
```

## 🌍 国际化脚本 (i18n/)

### `init_i18n.bat` - 初始化国际化
首次设置国际化支持，创建翻译文件。

**使用方式**：
```batch
.\scripts\i18n\init_i18n.bat
```

### `update_i18n.bat` - 更新翻译
提取最新的翻译文本并更新现有翻译文件。

**使用方式**：
```batch
.\scripts\i18n\update_i18n.bat
```

### `compile_i18n.bat` - 编译翻译
将 .po 翻译文件编译为 .mo 文件。

**使用方式**：
```batch
.\scripts\i18n\compile_i18n.bat
```

## 📝 注意事项

1. **路径**：所有脚本路径已更新，请使用新的路径格式
2. **工作目录**：脚本会自动切换到项目根目录
3. **权限**：某些脚本（如 `deploy.ps1`）可能需要管理员权限
4. **环境**：确保已激活虚拟环境或脚本会自动激活

## 🔗 相关文档

- [部署指南](../部署指南.md) - 完整部署说明
- [端口配置](../PORT_CONFIG.md) - 端口使用说明
- [国际化指南](../I18N_README.md) - 国际化使用说明

